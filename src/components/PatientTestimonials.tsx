export interface PatientTestimonialProps {
  authorName: string;
  rating: number;
  review: string;
}

const PatientTestimonialDiv = ({
  authorName,
  rating,
  review,
}: PatientTestimonialProps) => {};
