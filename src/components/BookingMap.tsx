import { MapContainer, TileLayer, Marker } from "react-leaflet";
import "leaflet/dist/leaflet.css";

const BookingMap = () => {
  const position = [43.64666, -79.394043]; // Latitude and longitude coordinates
  return <div></div>;
};

export default BookingMap;
